# Drop this file as .github/workflows/ci.yml in your TypeScript repo.
# It calls the shared CI workflow from codervisor/codervisor.
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    uses: codervisor/codervisor/.github/workflows/ci-typescript.yml@main
